# Needed to run the app and load the model (also what Streamlit Community Cloud installs).
# scikit-learn must match the version the model was trained with.
streamlit==1.64.0
pandas==3.0.5
numpy==2.5.3
scikit-learn==1.9.0
scipy==1.17.1
pyarrow==24.0.0
joblib==1.6.0
